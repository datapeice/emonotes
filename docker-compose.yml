services:
  emonotes:
    build: .
    container_name: emonotes_app
    env_file: .env
    ports:
      - 8080:8080
    depends_on:
      - postgres
    restart: always

  postgres:
    image: postgres:16
    container_name: emonotes_postgres
    env_file: .env
    volumes:
      - pgdata:/var/lib/postgresql/data
    restart: always

volumes:
  pgdata: